# Honeymoon Fund

## Product Spec

The canonical product spec lives at `~/Documents/my-os/honeymoon-fund/product-spec.md` (symlinked into this repo as `product-spec.md`).

When implementing features or making changes that affect product requirements, update the product spec to reflect the current state of what's been built, what's changed, and what's remaining.
